% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epicurve_ggplotly.R
\name{epicurve_ggplotly}
\alias{epicurve_ggplotly}
\title{Interactive epicurve via plotly with sensible defaults}
\usage{
epicurve_ggplotly(p, tooltip = "text", keep_symbol_legend = FALSE, ...)
}
\arguments{
\item{p}{A ggplot built with \code{\link[=geom_epicurve]{geom_epicurve()}}.}

\item{tooltip}{Passed to \code{\link[plotly:ggplotly]{plotly::ggplotly()}}; defaults to \code{"text"}
which uses the auto-generated tooltip.}

\item{keep_symbol_legend}{If \code{FALSE} (the default), hide the legend
for layers that use a custom symbol because plotly renders those
keys as \code{"Aa"}.}

\item{...}{Further arguments passed to \code{\link[plotly:ggplotly]{plotly::ggplotly()}}.}
}
\value{
A plotly htmlwidget.
}
\description{
Thin wrapper around \code{\link[plotly:ggplotly]{plotly::ggplotly()}} that fixes the rough edges
you get when converting an epicurve plot:
}
\details{
\itemize{
\item Uses the \code{text} aesthetic (auto-generated by \code{\link[=geom_epicurve]{geom_epicurve()}}) as
the sole tooltip source.
\item Strips the default \code{"trace 0"}, \code{"trace 1"}, ... suffix from hover
labels so tooltips show only the case/period information.
\item Re-injects the ggplot \code{subtitle} into the plotly title (plotly
silently drops \code{subtitle}).
\item For \code{\link[=geom_epicurve]{geom_epicurve()}} symbol mode, the ggplot legend keys are drawn
with the actual Unicode symbol; plotly cannot honour that custom
\code{draw_key}, so the legend is suppressed by default and the symbol
meaning is communicated via the hover tooltip instead. Pass
\code{keep_symbol_legend = TRUE} to keep plotly's default (ambiguous)
\code{"Aa"} legend.
}
}
\examples{
\dontrun{
library(ggplot2)
cases <- simulate_outbreak(n = 30, prop_missing = 0)
p <- ggplot(cases, aes(x = onset_date)) +
  geom_epicurve(fill = "steelblue") +
  scale_y_epicurve() +
  theme_minimal()
epicurve_ggplotly(p)
}

}
