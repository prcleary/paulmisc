% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epicurve_ggplotly.R
\name{epicurve_ggplotly}
\alias{epicurve_ggplotly}
\title{Interactive epicurve via plotly with sensible defaults}
\usage{
epicurve_ggplotly(
  p,
  tooltip = "text",
  keep_symbol_legend = FALSE,
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{p}{A ggplot built with \code{\link[=geom_epicurve]{geom_epicurve()}}.}

\item{tooltip}{Passed to \code{\link[plotly:ggplotly]{plotly::ggplotly()}}; defaults to \code{"text"}
which uses the auto-generated tooltip.}

\item{keep_symbol_legend}{If \code{FALSE} (the default), replace
plotly's auto-generated legend for symbol layers with a custom
annotation legend that shows the actual symbols (the auto legend
would render keys as the placeholder \code{"Aa"} glyph). Set to
\code{TRUE} to keep plotly's default behaviour.}

\item{width, height}{Optional widget dimensions in pixels. When
\code{height} is \code{NULL} (the default) the wrapper picks a height that
matches the static-plot aspect for datetime axes; for date /
numeric axes the htmlwidgets default is left alone.}

\item{...}{Further arguments passed to \code{\link[plotly:ggplotly]{plotly::ggplotly()}}.}
}
\value{
A plotly htmlwidget.
}
\description{
Thin wrapper around \code{\link[plotly:ggplotly]{plotly::ggplotly()}} that fixes the rough edges
you get when converting an epicurve plot:
}
\details{
\itemize{
\item Uses the \code{text} aesthetic (auto-generated by \code{\link[=geom_epicurve]{geom_epicurve()}}) as
the sole tooltip source.
\item Sets a per-trace hovertemplate so the default \code{"trace 0"},
\code{"trace 1"}, ... suffix never appears, and silences hover entirely
for traces that carry no useful tooltip (the geom_segment used by
\code{annotate_event()}, for example).
\item Re-injects the ggplot \code{subtitle} into the plotly title (plotly
silently drops \code{subtitle}).
\item Swaps the auto-added \code{\link[=coord_epicurve]{coord_epicurve()}} for \code{coord_cartesian()}
before conversion (plotly does not honour custom coords) and
instead applies the equivalent aspect by setting the widget
height, so hourly / sub-daily plots get the same shorter panel
they get in static ggplot2 output.
\item For \code{\link[=geom_epicurve]{geom_epicurve()}} symbol mode, plotly cannot render the custom
\code{draw_key} so the default legend swatch is the unhelpful \code{"Aa"}
glyph. The wrapper hides plotly's auto-legend and replaces it
with a small annotation block in the top-right of the panel that
lists each \verb{symbol category} mapping so the symbol-to-category
meaning is preserved.
}
}
\examples{
\dontrun{
library(ggplot2)
cases <- simulate_outbreak(n = 30, prop_missing = 0)
p <- ggplot(cases, aes(x = onset_date)) +
  geom_epicurve(fill = "steelblue") +
  scale_y_epicurve() +
  theme_minimal()
epicurve_ggplotly(p)
}

}
