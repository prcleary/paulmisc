% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epicurve_footnote.R
\name{epicurve_footnote}
\alias{epicurve_footnote}
\title{Summary footnote for an epicurve}
\usage{
epicurve_footnote(
  data,
  columns = NULL,
  show_missing = TRUE,
  show_timestamp = TRUE,
  timestamp_format = "\%d \%B \%Y \%H:\%M",
  extra = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame, typically the same one used to build the plot.}

\item{columns}{Optional character vector of columns to consider when
summarising missingness. Defaults to all non-ID columns in \code{data}.}

\item{show_missing}{Logical. Whether to include a missing-data summary
(default \code{TRUE}).}

\item{show_timestamp}{Logical. Whether to include a "produced at" stamp
(default \code{TRUE}).}

\item{timestamp_format}{A \code{format()} template used for the timestamp
(default \code{"\\\%d \\\%B \\\%Y \\\%H:\\\%M"}).}

\item{extra}{Optional character string appended verbatim to the caption.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:labs]{ggplot2::labs()}}.}
}
\value{
A \code{\link[ggplot2:labs]{ggplot2::labs()}} object suitable for adding to a ggplot.
}
\description{
Convenience helper that builds a \code{caption} summarising the missing-data
status of an epidemic curve and the time the chart was produced. Add the
result to a ggplot with \code{+}, e.g.
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{ggplot(cases, aes(x = onset_date)) +
  geom_epicurve() +
  epicurve_footnote(cases)
}\if{html}{\out{</div>}}

By default the footnote reports the proportion of rows with at least one
missing value across the supplied columns (or all columns when
\code{columns = NULL}) and stamps the chart with the current time. The text
can be customised or extended via \code{extra}.
}
\examples{
library(ggplot2)
cases <- simulate_outbreak(n = 50, seed = 1)
ggplot(cases, aes(x = onset_date)) +
  geom_epicurve(fill = "steelblue") +
  epicurve_footnote(cases)

}
