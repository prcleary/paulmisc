% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_outbreak.R
\name{simulate_outbreak}
\alias{simulate_outbreak}
\title{Simulate onset dates for a point-source disease outbreak}
\usage{
simulate_outbreak(
  n = 20,
  exposure = as.Date("2024-06-01"),
  meanlog = 1.6,
  sdlog = 0.45,
  seed = 42
)
}
\arguments{
\item{n}{Integer. Number of cases to simulate. Defaults to \code{20}.}

\item{exposure}{A \code{Date} (or object coercible to \code{Date}) giving the
common point-source exposure date. Defaults to \code{"2024-06-01"}.}

\item{meanlog, sdlog}{Numeric. Parameters of the log-normal incubation
period distribution on the log scale, passed to \code{\link[stats:Lognormal]{stats::rlnorm()}}.
The median incubation period is \code{exp(meanlog)} days, and \code{sdlog}
controls the spread. Defaults (\code{meanlog = 1.6}, \code{sdlog = 0.45}) give
a median of ~5 days with moderate spread, typical of many foodborne
pathogens. For quick-onset diseases like norovirus, try
\code{meanlog = 0.5} (median ~1.6 days). For slow-onset diseases like
hepatitis A, try \code{meanlog = 3} (median ~20 days).}

\item{seed}{Optional integer used to seed the random number generator
for reproducibility. Use \code{NULL} to leave the RNG state untouched.}
}
\value{
A data frame with one row per case and the columns
\code{case_id}, \code{onset_date}, \code{age_group}, \code{sex}, \code{outcome}, and \code{setting}.
}
\description{
Generates a small, realistic line list for a single point-source outbreak.
Incubation periods are drawn from a log-normal distribution (the classical
choice for many enteric and respiratory pathogens), producing the
right-skewed onset distribution typical of point-source events. A handful
of categorical case attributes (age group, sex, outcome, setting) are
simulated alongside, so that the resulting data frame can be used to
demonstrate or test plotting functions such as \code{\link[=geom_epicurve]{geom_epicurve()}}.
}
\examples{
cases <- simulate_outbreak()
head(cases)

# A larger outbreak with a different exposure date
big <- simulate_outbreak(n = 100, exposure = as.Date("2025-03-15"))
range(big$onset_date)

# Short incubation period (e.g., Salmonella)
# meanlog = 0.5 gives median of exp(0.5) = 1.6 days
fast <- simulate_outbreak(meanlog = 0.5, sdlog = 0.3)

# Long incubation period (e.g., Hepatitis A)
# meanlog = 3 gives median of exp(3) = 20 days
slow <- simulate_outbreak(meanlog = 3, sdlog = 0.5)

}
