% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_epicurve.R
\name{coord_epicurve}
\alias{coord_epicurve}
\title{Coordinate system for epidemic curves}
\usage{
coord_epicurve(clip = "on")
}
\arguments{
\item{clip}{Should drawing be clipped to the panel extent? Passed
through to \link[ggplot2:Coord]{ggplot2::CoordCartesian}. Defaults to \code{"on"}.}
}
\value{
A ggproto \link[ggplot2:Coord]{ggplot2::CoordCartesian} subclass.
}
\description{
A thin \link[ggplot2:Coord]{ggplot2::CoordCartesian} subclass that automatically constrains
the \emph{panel} aspect ratio when the x-axis is a datetime (POSIXct) so
that case rectangles look approximately square instead of tall, narrow
sticks on typical figure dimensions. For Date / numeric x axes it
behaves exactly like \code{coord_cartesian()} (no aspect is imposed).
}
\details{
\code{geom_epicurve()} adds \code{coord_epicurve()} automatically; users can opt
out with \code{auto_aspect = FALSE}, or override by adding their own
coordinate system (e.g. \code{coord_cartesian()}) after the geom.
}
